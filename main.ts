input.onButtonPressed(Button.A, function () {
	
})
let max = 15
let min = 2
let list: number[] = []
let array: number[] = []
for (let індекс = 0; індекс <= 4; індекс++) {
    array.insertAt(індекс, randint(min, max))
}
basic.forever(function () {
	
})
